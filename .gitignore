# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# jetbrains goodies
.idea

# macos stuff
.DS_Store
*/.DS_Store

# private dir for notes and such
private/
.private/

# log output
*.log

# vscode
.vscode
.devcontainer

# coverage
cover.out

# devspace cache
*.devspace

# direnv file
.envrc

# goreleaser dist dir for testing builds and such locally
.dist/

# build files
cmd/clabverter/build/
# ignore generated try-c9s state (tools + per-cluster rendered manifests),
# but keep the manifest templates committed at build/try-c9s/*.yaml
build/try-c9s/*/
# ignore downloaded e2e tool binaries, but keep build/e2e/kind.yaml committed
build/e2e/bin/

# for crd -> openapi gen
venv
