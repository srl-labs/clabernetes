version: v2beta1
commands:
  dev: "DEVSPACE_CONFIG=./.develop/devspace.yaml devspace dev --profile dev $@"
  deploy: "DEVSPACE_CONFIG=./.develop/devspace.yaml devspace deploy $@"
  build: "DEVSPACE_CONFIG=./.develop/devspace.yaml devspace build $@"
  purge: "DEVSPACE_CONFIG=./.develop/devspace.yaml devspace purge $@"
  pipeline: "DEVSPACE_CONFIG=./.develop/devspace.yaml devspace run-pipeline $@"
